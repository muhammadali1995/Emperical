package Solutions.linkedList;

import DataStructure.queue.QueueWithLinkedList.Queue;
import InterviewPrep.DataStucture.BinarySearchTree.TreeNode;

import java.util.LinkedList;

public class MaximumWidthBinaryTree {
    public int widthOfBinaryTree(TreeNode root) {
        LinkedList<Integer> bfTravQueue = new LinkedList<>();
        bfTravQueue.push(root.data);
        while (root!=null){
//         bfTravQueue.push(root.left);
        }
        return 0;
    }
}
