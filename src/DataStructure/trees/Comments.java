package DataStructure.trees;

/**
 * Created by User on 8/3/2018.
 */
public class Comments {
    /**
     * Trees are dynamic data structures
     *
     * Terminology
     * root node -> where the tree starts
     * parent node -> the node that does not have a parent
     *
     * child node ->the node that has both parent and child
     * leaf node -> the node that does not have a child
     *
     * no cycles in a tree
     *
     * single root
     *
     *
     * */


    /**
    * Binary trees
     * Any parent can have at most two children
    *
    * */
}
